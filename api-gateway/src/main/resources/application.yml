server:
  port: 8080

spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      globalcors:
        corsConfigurations:
          '[/**]':
            allowedOriginPatterns:
              - "http://localhost:*"
            allowedMethods:
              - GET
              - POST
              - PUT
              - DELETE
              - OPTIONS
            allowedHeaders:
              - "*"
            allowCredentials: true
      routes:
        - id: auth-service
          uri: http://localhost:8081
          predicates:
            - Path=/auth/**
        - id: event-service
          uri: http://localhost:8082
          predicates:
            - Path=/api/events/**
        - id: booking-service
          uri: http://localhost:8083
          predicates:
            - Path=/booking/**
        - id: queue-service
          uri: http://localhost:8084
          predicates:
            - Path=/queue/**

jwt:
  secret: ticketrushJwtSecretKeyMustBeAtLeastThirtyTwoBytesLong2026
